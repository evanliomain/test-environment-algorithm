define(function() {
  'use strict';
  return {
    /**
     * Get the length of an array
     * @param  {Array} a
     * @return {integer} the item count of a
     */
    length : function(a) {
      // TODO
      return null;
    },
    /**
     * Test if an anrray is empty
     * @param  {Array} a
     * @return {Boolean} true if a is empty, false else
     */
    isEmpty : function(a) {
      // TODO
      return null;
    },
    /**
     * Sort the given array
     * @param  {Array} a
     * @return {Array}   a new sorted array
     */
    sort : function(a) {
      // TODO
      return null;
    },

    decimalToBinary : function(n) {
      // TODO
      return null;
    },
    /**
     * Convert an array of 0, 1 into a integer
     * @param  {Array of 0,1} a
     * @return {integer}  the representation in decimal form
     */
    binaryToDecimal : function(a) {
      // TODO
      return null;
    },
    /**
     * Test if the array is a palindrom :
     * a man, a plan, a canal: a Panama
     * @param  {Array}  a
     * @return {Boolean}  true if a is a palindrom, false else
     */
    isPalindrom : function(a) {
      // TODO
      return null;
    }
  };
});
