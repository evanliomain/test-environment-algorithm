define(function() {
  'use strict';
  return {
    /**
     * Get the integer n^m, or n*n*n*...*n (m times)
     * @param  {integer} n the indice
     * @param  {integer} m the exposant
     * @return {integer} n^m
     */
    pow : function(n, m) {
      return 0;
    }
  };
});
